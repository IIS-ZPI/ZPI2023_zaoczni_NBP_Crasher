import { ApiError } from '../../models/error.model';

export const statsApiErrors: ApiError = {
  'invalid_data': 'Invalid data',
  'date_not_supported': 'Date not supported',
  'currencies_are_equal': 'Selected currencies are equal',
};
